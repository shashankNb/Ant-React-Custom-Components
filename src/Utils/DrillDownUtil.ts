import {NavigateFunction} from "react-router-dom";
import axios from "../core/api.ts";

export const DrillDownUtil = {
    drill: (navigate: NavigateFunction, state: {[key: string]: any}, url: string, apiUrl: string) => {
        // axios.post()
    }
}